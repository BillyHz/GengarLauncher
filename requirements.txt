# GengarLauncher – Python 3.10 dependencies
# Install with: py -3.10 -m pip install -r requirements.txt

customtkinter==5.2.2        # Dark-mode GUI framework (tkinter wrapper)
darkdetect==0.8.0           # Detects OS dark/light theme (used by customtkinter)
minecraft-launcher-lib==8.0 # Mojang API wrapper: version list, install, launch commands
requests==2.34.2            # HTTP client for JDK & asset downloads
